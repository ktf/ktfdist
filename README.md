# How to use

Have your usual alidist setup.

Clone ktfdist somewhere, e.g. in `~/src/ktfdist`.

Set up `export BITS_PATH=~/src/ktfdist`.

Build rooture as usual with:


```bash
aliBuild build --defaults o2 rooture
```



